// Placeholder for MediaPipe LLM + LiteRT model integration
class ModelIntegrationService {
  Future<Map<String, dynamic>> runTriageModel({required String text, String? imageBase64}) async {
    // TODO: Integrate on-device Gemma 4 model
    return Future.value({});
  }
}
